# babel plugin experiment

to setup:

```bash
cd babel-plugin-experiment
# ... point lerna.json to babel source
npx lerna bootstrap
```

to build:

```bash
npm run build
```

to watch:

```bash
npm run watch
```
